<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pytap.project.dao.WebLogDao">

    <insert id="insertWebLog">
        INSERT INTO `web_log`(
            description, username, method, spend_time, ip, result, create_time)
        VALUES
            (#{description}, #{username}, #{method}, #{spendTime}, #{ip}, #{result}, #{createTime})
    </insert>
    <select id="getWebLog" resultType="com.pytap.project.dto.WebLogDTO">
        SELECT id, description, username, method, spend_time, ip, result, create_time
        FROM `web_log`
        <where>
            <if test="id != null and id != ''">
                id = #{id},
            </if>
            <if test="username != null and username != ''">
                username = #{username},
            </if>
            <if test="method != null and method != ''">
                method = #{method},
            </if>
            <if test="ip != null and ip != ''">
                ip = #{ip}
            </if>
        </where>
    </select>
    <select id="listWebLogs" resultType="com.pytap.project.dto.WebLogDTO">
        SELECT id, description, username, method, spend_time, ip, result, create_time FROM `web_log`
    </select>
    <delete id="deleteAllWebLogs">
        DELETE FROM `web_log`
    </delete>
</mapper>