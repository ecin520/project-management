<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pytap.project.dao.UserAddPermissionDao">

    <insert id="insertUserAddPermission">
        INSERT INTO user_additional_permission (user_id, additional_permission_id, create_time)
        VALUES (
            #{userId}, #{additionalPermissionId}, #{createTime}
        )
    </insert>
    <update id="updateByUserAddPermissionId">
        UPDATE user_additional_permission SET user_id = #{userId}, additional_permission_id = #{additionalPermissionId}, create_time = #{createTime}
    </update>
    <delete id="deleteByUserAddPermission">
        DELETE FROM user_additional_permission
        <trim suffixOverrides=",">
            <where>
                <if test="id != null">
                    id = #{id}
                </if>
                <if test="userId != null and additionalPermissionId != null">
                    user_id = #{userId} AND additional_permission_id = #{additionalPermissionId}
                </if>
            </where>
        </trim>
    </delete>
    <select id="getByUserAddPermissionId" resultType="com.pytap.project.entity.UserAddPermission">
        SELECT id, user_id, additional_permission_id, create_time FROM user_additional_permission WHERE id = #{id}
    </select>
    <select id="listAllUserAddPermissions" resultType="com.pytap.project.entity.UserAddPermission">
        SELECT id, user_id, additional_permission_id, create_time FROM user_additional_permission
    </select>
    <select id="countAddPermission" resultType="java.lang.Integer">
        SELECT count(*) FROM user_additional_permission
    </select>
</mapper>